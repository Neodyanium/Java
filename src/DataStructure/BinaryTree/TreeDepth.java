package DataStructure.BinaryTree;

public class TreeDepth {

    /*How to:You can implement a recursive function with relation 1+max(l,h)
        keep calling the function while sending the left subtree and rightsubtree with base condition
        when either of node is null simply return 0;
     */
    public int depth(TreeNode root){
        return 0;
    }
}
