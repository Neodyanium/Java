package DataStructure.BinaryTree;

public class BalancedBinaryTree {

    //Difference between Height of left subtree and right subtree should be at max 1;
    //one way to do it calculate height of left
    //recursively check the height of left and right subtree at anypoint if any of height is -1 return false
    // check if the absolute difference between left and right subtree is > 1 return -1;
    public static boolean bbt(TreeNode node) {
        return true;
    }
}
